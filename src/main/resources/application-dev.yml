spring:
  jpa:
    show-sql: true

logging:
  level:
    root: INFO
    com.zyelectric.ocpp: DEBUG
    org.hibernate.SQL: DEBUG

server:
  error:
    include-message: always
    include-stacktrace: on_param
